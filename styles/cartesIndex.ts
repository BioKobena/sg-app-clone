import { Colors } from "@/constants/Colors";
import { StyleSheet } from "react-native";

export const stylesIndexCard = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: Colors.blanc,
        padding: 15,
        alignItems: "center",
        // justifyContent: "center",
    },
});
